<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vlad.passKeeper">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PassKeeper">
        <activity
            android:name=".ui.SplashScreen"
            android:theme="@style/Theme.PassKeeper.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/Theme.PassKeeper.Launcher" />
        <activity
            android:name=".ui.RegisterActivity"
            android:theme="@style/Theme.PassKeeper.Launcher" />
        <activity android:name="com.vlad.passKeeper.MainActivity" />
        <activity android:name="com.vlad.passKeeper.ui.PasswordAdd" />
        <activity android:name="com.vlad.passKeeper.ui.SearchActivity" />
    </application>

</manifest>